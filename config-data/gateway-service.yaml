server:
  port: 8080
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://backendeureka:8761/eureka
  instance:
    hostname: backendeureka